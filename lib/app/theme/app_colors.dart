import 'package:flutter/material.dart';

final class AppColors {
  const AppColors._();

  // Ana marka renkleri
  static const primary = Color(0xFF182958);
  static const primaryDark = Color(0xFF0D1735);
  static const secondary = Color(0xFFF1743A);

  // Destekleyici vurgu renkleri
  static const purple = Color(0xFF7C64D5);
  static const teal = Color(0xFF008C95);

  // Durum renkleri
  static const success = Color(0xFF2E9D57);
  static const warning = Color(0xFFF2A93B);
  static const error = Color(0xFFD94D4D);

  // Açık tema
  static const background = Color(0xFFF7F8FA);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF161A24);
  static const textSecondary = Color(0xFF667085);

  // Koyu tema
  static const darkBackground = Color(0xFF101218);
  static const darkSurface = Color(0xFF191C24);
}
